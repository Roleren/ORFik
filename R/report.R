#' Full ORFik report
#' @param experiment an ORFik experiment data.frame
#' @param outputDir directory to save output, default (.)
#' @return NULL (all output stored to disc and .GlobalEnv)
ORFikReport <- function(experiment, outputDir = ".") {
  return(NULL)
}
